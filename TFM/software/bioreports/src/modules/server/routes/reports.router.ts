import * as express from 'express';

const reportsRouter = express.Router();

/**
 * Primary app routes.
 */
// app.get("/", homeController.index);
/*
reportsRouter.get("/login", userController.getLogin);
reportsRouter.post("/login", userController.postLogin);
reportsRouter.get("/logout", userController.logout);
reportsRouter.get("/forgot", userController.getForgot);
reportsRouter.post("/forgot", userController.postForgot);

export reportsRouter;
*/